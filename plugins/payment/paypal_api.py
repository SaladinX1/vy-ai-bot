class PayPalAPI:
    def create_payment_link(self, amount, currency="USD"):
        # Simulé — nécessite une vraie API PayPal (OAuth + endpoints)
        return f"https://paypal.com/pay?amount={amount}&currency={currency}"
